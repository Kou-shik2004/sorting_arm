# display stack for the deployment image - no host X server, so we bring our own

# not novnc - the .deb hard-depends on nodejs and we only want the static
# client, fetched straight from upstream in the novnc-fetch stage instead

fonts-dejavu-core
libgl1-mesa-dri
openbox
python3-websockify
x11vnc
xvfb
