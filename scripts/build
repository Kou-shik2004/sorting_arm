#!/usr/bin/env bash

# Builds the dev image. Doesn't start anything.

set -euo pipefail
source "$(dirname "${BASH_SOURCE[0]}")/_lib.sh"
cd "$REPO_ROOT"

require_docker
$DOCKER_CMD compose -f docker-compose.yaml build
